version: '3.8'
services:
  backend-tests:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      db:
        condition: service_healthy
    environment:
      - POETRY_VIRTUALENVS_IN_PROJECT=true
      - POETRY_CACHE_DIR=/tmp/poetry_cache
#      - DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/licitacao_hub
#      - TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/test_db
    volumes:
      - .:/app  # Monta o código atual no container
      - /tmp/poetry_cache:/tmp/poetry_cache
    working_dir: /app
    command: >
      sh -c "rm -rf /tmp/poetry_cache &&
             python -m pip install --upgrade pip &&
             poetry config virtualenvs.in-project true &&
             poetry install --no-interaction --no-ansi &&
             python -m pytest tests/"
  
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: licitacao_hub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: